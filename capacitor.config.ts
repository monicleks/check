import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mYaplicacionton',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
